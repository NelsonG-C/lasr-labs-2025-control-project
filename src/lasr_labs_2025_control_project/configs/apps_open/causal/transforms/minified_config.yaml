experiment_folder: experiments/apps_open/causal

experiment_name: causal

results_folder: results/minified/

defaults:
  transform:
      type: minifier
      remove_comments: true

runs:
  attacks:
    eval_path: generator/attacks.eval
    save_path: transforms/minified/attack_transformed.eval

  honest:
    eval_path: generator/honest.eval
    save_path: transforms/minified/honest_transformed.eval

  honeypots:
    eval_path: generator/honeypots.eval
    save_path: transforms/minified/honeypots_transformed.eval
